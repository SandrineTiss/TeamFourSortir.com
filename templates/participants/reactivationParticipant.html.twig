{% extends 'base.html.twig' %}

{% block stylesheets %}
    <link rel="stylesheet" href="{{  asset('css/participation.css') }}">
{% endblock %}

{% block title %}Activation participants{% endblock %}

{% block body %}

    <h1>Etes-vous sûr de vouloir activer ce participant ?</h1>

    {{ form_start(participant) }}
    <div class="cache">
        {{ form_row(participant.image) }}
    </div>

    {{ form_widget(participant) }}
    <button>Activer ce participant</button>
    {{ form_end(participant) }}

{% endblock %}