<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Sortie.com{% endblock %}</title>
        <link rel="preconnect" href="https://fonts.googleapis.com">
        <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
        <link href="https://fonts.googleapis.com/css2?family=Roboto+Mono&display=swap" rel="stylesheet">
        <link href="https://fonts.googleapis.com/css2?family=Merriweather:wght@700&display=swap" rel="stylesheet">
        <link href="https://fonts.googleapis.com/css2?family=Lobster&display=swap" rel="stylesheet">
        <link href="https://fonts.googleapis.com/css2?family=Architects+Daughter&display=swap" rel="stylesheet">
        <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.1/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-F3w7mX95PdgyTmZZMECAngseQB83DfGTowi0iMjiWaeVhAn4FJkqJByhZMI3AhiU" crossorigin="anonymous">
        <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.1/dist/js/bootstrap.bundle.min.js" integrity="sha384-/bQdsTh/da6pkI1MST/rWKFNjaCP5gBSY4sEBT38Q/9RBh9AH40zEOg7Hlq2THRZ" crossorigin="anonymous"></script>
        <link rel="stylesheet" href="{{  asset('css/normalize.css') }}">
        <link rel="stylesheet" href="{{  asset('css/app.css') }}">
        {% block stylesheets %}
        {% endblock %}
    </head>
    <body>
        <header>
            <div class="container-fluid px-1">
                <nav class="navbar">
                    <div class="col-md-12">
                        <div class="row g-0">
                            {% if app.user %}
                                <a class="col-lg-2 col-md-1 col-sm-3" href="{{ path('main_accueil') }}"><img class="logo" src="{{ asset('img/logo1.png') }}" alt="Accueil"></a>

                                {% if is_granted('ROLE_ADMIN') %}
{#                                <a class="col-lg-1 col-md-1 col-sm-3" href="{{ path('campus_affichage') }}">Gestion des Campus</a>#}
{#                                <a class="col-lg-1 col-md-1 col-sm-3" href="{{ path('ville_affichage') }}">Gestion des Villes</a>#}
{#                                <a class="col-lg-1 col-md-1 col-sm-3" href="{{ path('lieu_affichage') }}">Gestion des Lieux</a>#}
{#                                <a class="col-lg-1 col-md-1 col-sm-3" href="{{ path('participants_gestion') }}">Gestion des participants</a>#}
                                    <a href="{{ path('admin_access') }}" class="col-lg-1 col-md-1 col-sm-3">Administrateur</a>
                                {% endif%}

                                <a class="col-lg-1 col-md-1 col-sm-3" href="{{ path('sortie_creer') }}">Creer une sortie</a>
                                <a class="col-lg-1 col-md-1 col-sm-3" href="{{ path('profil_user') }}">Mon profil</a>
                                <a class="col-lg-1 col-md-1 col-sm-3" href="{{ path('app_logout') }}">Déconnexion</a>
                            {% endif %}
                        </div>
                    </div>
                </nav>
            </div>
        </header>

        {% for label, messages in app.flashes %}
            {% for message in messages %}
                <div class="alert-{{ label }} flash">
                    {{ message }}
                </div>
            {% endfor %}
        {% endfor %}

        <div class="container-fluid">
            {% block body %}{% endblock %}
        </div>
        {% block js %}{% endblock %}
        {% block footer %}
            <footer class="py-3 footer">
                <div class="container">
                    <p class="m-0 text-center">Copyright &copy; ENI Ecole ; TeamFour Sortir.com
                        2021</p>
                </div>
            </footer>
        {% endblock %}

    {% block javascript %}{% endblock %}

    </body>
</html>
