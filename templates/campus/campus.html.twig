{% extends 'base.html.twig' %}

{% block stylesheets %}
    <link rel="stylesheet" href="{{  asset('css/campus.css') }}">
{% endblock %}

{% block title %}Gestion des campus{% endblock %}

{% block body %}

    <h1>Gestion des campus</h1>

    {% for campus in listeCampus %}
        - {{ campus.nom }} - <a class="petit" href="{{ path('campus_modifier', {'id': campus.id }) }}" > Modifier </a> - <a class="petit" href="{{ path('campus_suppression', {'id': campus.id }) }}" > Supprimer </a> <br/>
    {% endfor %}

    <a href="{{ path('campus_creation') }}"><button>Ajouter un campus</button></a>

{% endblock %}
